import { Router } from 'express';
import <%= props.moduleNameUpper %> from './<%= props.moduleNameLower %>.controller';

export default function routes(
  route: Router,
  baseUrl: string,
  middleware?: any,
): void {
  route.post(`${baseUrl}/store`, <%= props.moduleNameUpper %>.store);
  route.get(`${baseUrl}/index/:id`, <%= props.moduleNameUpper %>.index);
  route.get(`${baseUrl}/show`, <%= props.moduleNameUpper %>.show);
  route.delete(`${baseUrl}/delete/:id`, <%= props.moduleNameUpper %>.delete);
  route.put(`${baseUrl}/update/:id`, <%= props.moduleNameUpper %>.update);
}
