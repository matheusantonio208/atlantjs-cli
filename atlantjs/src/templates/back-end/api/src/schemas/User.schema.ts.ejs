import { model, Schema } from 'mongoose';

import { UserCreatedDto } from '../modules/User/dto/index.dto';

const userSchema = new Schema<UserCreatedDto>(
  {
  //! properties-start
    property: String,
  //! properties-end
},
  { timestamps: true },
);

export default model<UserCreatedDto>('users', userSchema);
