class UserService {
  // services-start
  serviceFunction() {
    const greetings = 'Hi! I am a'
    const service = 'service'

    console.log(`${greetings} ${service}`);

    return {userProperty: `${greetings} ${service}`}
  }
}

// services-end
export default new UserService();
