
export class UserToUpdateDto {
  //! properties-start
  first_name: string;
  last_name: string;
  email: string;
  password_hash: string;
  photo_profile?: string;
  date_birth?: string;
  phone_number?: string;
  country?: string;
  state: string;
  group: string;
  date_last_login?: string;
  locale_last_login?: string;
  account_status: string;
  //! properties-end

  constructor(body: UserToUpdateDto) {
    //! constructor-start
    this.first_name = body?.first_name
    this.last_name = body?.last_name
    this.email = body?.email
    this.password_hash = body?.password_hash
    this.photo_profile = body?.photo_profile
    this.date_birth = body?.date_birth
    this.phone_number = body?.phone_number
    this.country = body?.country
    this.state = body?.state
    this.group = body?.group
    this.date_last_login = body?.date_last_login
    this.locale_last_login = body?.locale_last_login
    this.account_status = body?.account_status
    //! constructor-end
  }
}
