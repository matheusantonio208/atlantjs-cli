class <%= props.moduleNameUpper %>Service {
// services-start
  serviceFunction() {
    const greetings = 'Hi! I am a'
    const service = `<%= props.moduleNameUpper %>`

    console.log(`${greetings} ${service}`);

    return {<%= props.moduleNameUpper %>Property: `${greetings} ${service}`}
  }
}
// services-end
export default new <%= props.moduleNameUpper %>Service();
