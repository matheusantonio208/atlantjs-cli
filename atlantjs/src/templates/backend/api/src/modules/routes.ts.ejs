import { Router } from 'express';

//! imports-start

//! imports-end

class Routes {
  route: Router;

  constructor() {
    this.route = Router();
    //! routes-start

    //! routes-end
  }
}
export default new Routes().route;
